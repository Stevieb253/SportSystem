// strike_zone.js — Stub (skipped per user request). D3 zone viz can be added later.

function drawStrikeZone(containerId, pitches) {
  // Placeholder — strike zone visualizer deferred
}

function updateZone(containerId, newPitches) {
  // Placeholder
}

function clearZone(containerId) {
  const el = document.getElementById(containerId);
  if (el) el.innerHTML = '';
}
